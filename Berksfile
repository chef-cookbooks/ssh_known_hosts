source 'https://supermarket.chef.io'

metadata

group :unit do
  cookbook 'ssh_known_hosts_test', path: 'test/fixtures/cookbooks/ssh_known_hosts_test'
end

group :integration do
  cookbook 'apt'
  cookbook 'minitest-handler'
end
